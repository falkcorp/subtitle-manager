### Changed

#### Add a maintained next-session prompt and correct the provider count

`docs/NEXT_SESSION_PROMPT.md` is a self-contained handoff prompt to paste into a
fresh agent session — current state, what to pick up next, the two findings
raised but deliberately not fixed, and the working conventions this repo
expects. It is meant to be updated at the end of each session rather than
rewritten from scratch.

The parity matrix's provider-registry row still said "4 real"; wizdom makes it
five, and the keyless track is now exhausted on probe evidence rather than on
assumption.
